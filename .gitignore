# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# window excel and word lock files
~$*

# credential files
credential.json
aps_credentials.json

# ExeProducts: product exes are tracked in git history.
# App-specific credentials and OAuth tokens (do not commit)
Apps/_revit/EnneaDuck.extension/EnneadTab Tailor.tab/Proj. 2534.panel/NYU HQ.pulldown/nyu_hq_daily_publisher/configs/credentials.json
Apps/_revit/EnneaDuck.extension/EnneadTab Tailor.tab/Proj. 2534.panel/NYU HQ.pulldown/nyu_hq_daily_publisher/configs/user_oauth.json

# InDesign lock files
*.idlk

# Rhino backup files
*.3dmbak

# C extensions
*.so

# DOM capture folder
DarkSide/ei_pdf_uploader/DOM capture/


# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
.python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Never ignore anything in Apps\_engine directory
!Apps\_engine/
!Apps\_engine/**

# Prevent duplicate docs folders
Website/docs/

**/current_job.sexyDuck
DarkSide/exes/source code/RevitSlave-3.0/revit_logic/current_check.txt

# AutoExporter orchestrator runtime files
Apps/_revit/EnneaDuck.extension/EnneadTab.tab/ACE.panel/AutoExporter.pushbutton/current_job_payload.json
Apps/_revit/EnneaDuck.extension/EnneadTab.tab/ACE.panel/AutoExporter.pushbutton/current_job_status.json

# SparcHealth orchestrator runtime files
Apps/_revit/EnneaDuck.extension/EnneadTab Tailor.tab/Proj. 2412.panel/Sparc.pulldown/SparcHealth.pushbutton/current_job_payload.json
Apps/_revit/EnneaDuck.extension/EnneadTab Tailor.tab/Proj. 2412.panel/Sparc.pulldown/SparcHealth.pushbutton/current_job_status.json
Apps/_revit/EnneaDuck.extension/EnneadTab Tailor.tab/Proj. 2412.panel/Sparc.pulldown/SparcHealth.pushbutton/heartbeat/
Apps/_revit/EnneaDuck.extension/EnneadTab Tailor.tab/Proj. 2412.panel/Sparc.pulldown/SparcHealth.pushbutton/orchestrator_logs/

# Debug and temporary files
DEBUG/

# RevitSlave3 debug/status files (should be in DEBUG or task_output)
SCRIPT_STARTED_*.txt
OPEN_ERROR_*.txt
METRICS_ERROR_*.txt
current_check.txt
current_job_status.json
DarkSide/exes/source code/RevitSlave-4.0/revit_logic/current_job.sexyDuck
*.tmp
*.lock
temp_*.py
test_*.py
*_temp.*
*_test.*

# AutoDist state files (machine-local, publish status reported to EnneadTab-Sync API)
DarkSide/publish/publish_history.json
DarkSide/publish/scheduler_state.json
# ...and the local backup OF that file. It was the only one of the publisher's
# five state files still tracked -- an oversight, not a decision: the live file
# was ignored while its own .bak was committed. It carries no publish
# success/failure, only scheduler bookkeeping (last tick, pending commits, last
# published sha), so it was never the cross-machine status channel it looked
# like. What dev machines actually want is already served twice over:
# EA_Dist/Apps/lib/EnneadTab/DIST_VERSION.json (version + source_commit, on every
# machine) and the InfraWatch /infra/api/ingest/publish-status DB (success,
# error_reason, traceback, machine).
DarkSide/publish/scheduler_state.json.bak
DarkSide/token.json
# ...and the ROOT one, which was tracked for 10 months. Same oversight shape as
# scheduler_state.json.bak above: the sibling path was ignored, this one was not.
# It held a 93-char github_pat_* string, was read by nothing, and got committed on
# 2025-09-15 by the AutoDist daemon's `git add -A` -- not by anyone deciding to.
# Untracked 2026-07-13. Any credential that ever sat here must be treated as
# disclosed and rotated at the provider; removing a secret from the tree is not
# rotating it.
token.json

# Auto-generated analysis files
longest_paths.json

# Area monitoring generated files
**/monitor_area.pushbutton/reports/*.html
!**/monitor_area.pushbutton/reports/.gitkeep
**/monitor_area.pushbutton/area_geometry_debug.json
**/monitor_area.pushbutton/*.json
!**/monitor_area.pushbutton/hardware_cache.json
!**/monitor_area.pushbutton/config.json



Apps/generate_zodiacs.py

# Runtime state (regenerated by scheduler)
DarkSide/publish/publish_run_state.json
DarkSide/publish/scheduler_heartbeat.json
DarkSide/publish/last_tick_run.txt
DarkSide/publish/logs/

# Artifacts of importing the EnneadTab library on a non-Windows machine (CI,
# agents, anyone running the check_*.py tests). ENVIRONMENT.py builds these
# paths with a literal "\", which is a path separator on Windows but part of
# the FILENAME everywhere else -- so an import creates an empty "{}" file next
# to the real, tracked ones. Ignore the mangled twins, never the originals.
Apps/_revit\\knowledge_revit_database.sexyDuck
Apps/_rhino\\knowledge_rhino_database.sexyDuck
